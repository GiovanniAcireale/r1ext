{
  "name": "r1ext",
  "displayName": "r1Extension",
  "description": "DeepSeek R1 VS Code extension",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.70.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:r1ext.askR1"
  ],
  "main": "./extension.js",
  "contributes": {
    "commands": [
      {
        "command": "r1ext.askR1",
        "title": "Ask r1"
      }
    ]
  },
  "scripts": {
    "lint": "eslint .",
    "pretest": "npm run lint",
    "test": "echo \"No tests configured\""
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.70.0",
    "@vscode/test-electron": "^2.4.1",
    "eslint": "^8.0.0"
  },
  "dependencies": {
    "vsce": "^1.97.0"
  }
}
